import { log } from "../utils/logger";

export class BackendWebSocket {
    connect(): void {
        log("WebSocket connection not implemented yet.");
    }

    disconnect(): void {
        log("WebSocket disconnected.");
    }
}